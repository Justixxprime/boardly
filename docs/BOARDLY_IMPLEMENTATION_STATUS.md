# Boardly 2.0: Implementation Status

Per brief Section 93. Never marks a feature "Production Ready" just
because a UI exists. Updated as each slice actually ships.

| Feature | Status | Backend | Frontend | Tested | Production Ready |
|---|---|---|---|---|---|
| **Invoice payments switched from Paystack to Squad (22 Sep 2026)** | Building | `create-invoice-payment` now calls Squad's `/transaction/initiate` and picks sandbox vs live base URL from the key prefix; `invoice-payment-webhook` and the invoice half of `payment-webhook` now check Squad's `x-squad-encrypted-body` signature instead of Paystack's. Marketplace bookings (client charge and provider payout) are untouched, still Paystack, since payout is not yet built for Squad. Needs `SQUAD_SECRET_KEY` set as a Supabase secret, and `payment-webhook`'s URL registered as the Webhook URL in Squad's dashboard (in addition to Paystack's, the function tells the two apart by header) | n/a | Not tested end to end with a real Squad sandbox payment yet | No |
| The rows below describe the invoice payment rows as they were when only Paystack existed; superseded for invoices by the row above, kept for history | n/a | n/a | n/a | n/a | n/a |
| Phase 0, Audit (`BOARDLY_2_IMPLEMENTATION_MAP.md`) | Existing | n/a | n/a | n/a | Yes |
| Font decision (General Sans, then Synonym, then Geist Sans, chosen live by Charles) | Existing | n/a | Yes (all 28 pages) | Scripted verification only | Yes |
| Design tokens: spacing/radius scales, money/client/work colors | Existing | n/a | Yes (`css/style.css`) | Not visually audited yet | No |
| `.modal-backdrop`/`.modal-panel`/`.sheet`/`.table`/`.loading-state`/`.error-state` | Existing | n/a | Yes; `.modal-panel`/`.table` now used live by Money Center | Used in money.html, not cross-browser audited | No |
| `docs/BOARDLY_DESIGN_SYSTEM.md` / `docs/BOARDLY_IMPLEMENTATION_STATUS.md` | Existing | n/a | n/a | n/a | Yes (docs themselves complete) |
| Existing 24-modal inventory migrated to `.modal-panel` | Planned | n/a | No | No | No |
| **Money Foundation, invoices table and transactions ledger** (`schema_v62_money_foundation.sql`) | Existing | Applied and confirmed live (both tables present, RLS enabled, owner-only policies confirmed) | n/a | Confirmed via `list_tables` and a column check; not yet exercised by a real invoice | No |
| **Invoice payments state machine** (`schema_v63_invoice_payments.sql`, adds `transactions.status`) | Existing | Applied and confirmed live (`status` column present, default `confirmed`, existing manual-entry behavior unchanged) | n/a | Confirmed via a column check | No |
| **`get-invoice-info` edge function** | Existing | Deployed live (version 2), now filters to confirmed transactions and returns balance/payable | n/a | Not tested end to end against a real invoice yet | No |
| **`create-invoice-payment` edge function** (starts a real Paystack checkout) | Existing | Deployed live, mirrors `marketplace-create-booking`'s proven pattern: server-computed balance, pending transaction row, Paystack initialize call | n/a | Not tested end to end. Needs `PAYSTACK_SECRET_KEY` configured to actually work | No |
| **`payment-webhook` edge function** (combined router, one URL for both Marketplace and Money) | Existing | Deployed live, solves the one-webhook-per-Paystack-account limit by checking the payment reference against both `marketplace_bookings.id` and `transactions.idempotency_key`, safe since those id spaces never overlap | n/a | Not tested against a real Paystack webhook yet. This is the URL to register in Paystack now, not the two older single-purpose ones | No |
| `invoice-payment-webhook` edge function (still deployed, superseded by `payment-webhook` above) | Existing | Deployed live but no longer the one to register in Paystack | n/a | Not tested | No |
| **`invoice.html` / `js/invoice-page.js`** (public client view, now with a real Pay now button) | Building | n/a | Yes | Not tested against live data yet | No |
| **`money.html` / `js/money.js`** (Invoices/Expenses/Ledger/Profitability, owner side) | Building | n/a | Yes; ledger and summary strip now correctly exclude pending/failed gateway attempts from totals; overdue detection now real (deterministic due-date and balance check on every load, not AI, see Section 83); Ledger tab entries can now be deleted directly (useful for cleaning up test data); money figures switched from the monospace/terminal look to a cleaner sans-serif with tabular numbers, per feedback that the mono style read as too "coder tool" | Not tested against live data yet | No |
| Money Foundation, profitability view (Section 10) | Existing | `schema_v65_profitability.sql` applied and confirmed live (adds `boards.hourly_rate`, nullable, no default so nothing is silently costed at zero) | n/a | Confirmed via a column check | No |
| **Profitability tab in `money.html`** (revenue, expenses, tracked time, labour cost, profit, margin per project) | Building | n/a | Yes; pulls from invoices, confirmed transactions, and the existing `time_entries` table; margin banding uses fixed 40%/15% thresholds, not AI, per Section 83 | Not tested against live data yet | No |
| **Client CRM foundation** (`schema_v64_clients.sql`, `clients` table, `invoices.client_id`) | Existing | Applied and confirmed live (table and column both present, RLS enabled, owner-only policy) | n/a | Confirmed via a schema check | No |
| **`clients.html` / `js/clients.js`** (client list, add/edit, computed billed/received/outstanding per client) | Building | n/a | Yes; Clients added to top nav across all pages | Not tested against live data yet | No |
| **Invoice builder client picker** (link an invoice to a saved client, auto-fill name/email) | Building | n/a | Yes; guarded so invoice saving still works even if `schema_v64` isn't run yet | Not tested against live data yet | No |
| **Navigation rebuild, Phase 2** (Home/Work/Clients/Money/Discover/Insights) | Existing | n/a | 6 of the brief's 7 sections now have a real destination and appear consistently in both desktop and mobile nav across every hub page (home, dashboard, clients, money, marketplace, stats, settings, tools): Home, Work (dashboard), Clients, Money, Discover (marketplace), Insights (stats), plus Settings. Fixed a pre-existing naming collision where "Home" already meant the public marketing page (`index.html`) in every mobile menu; that link is now labeled "Marketing site" and Home means the new authenticated `home.html`. **Operations is deliberately NOT added** as a top-level destination: per the Phase 0 audit, the vertical workflows (Classroom, Dispatch, Care Rounds, Content Calendar, Dev Board) are board *types* selected inside Work, not separate pages or a standalone module system (that's Phase 7's persona/module work, not built yet), so a top-level "Operations" link would have nowhere real to point without either duplicating Work's board-switcher or being a fake placeholder | Confirmed via scripted duplicate-ID and div-balance checks on every touched file | No |
| **Home, Phase 3** (`home.html`/`js/home.js`, Section 6) | Existing | n/a | Attention/Today/Money/Work/Clients/Momentum sections, every number a plain query or plain arithmetic over tasks, invoices, transactions, and clients, none of it AI, matching Section 83. Sections degrade gracefully to "n/a" rather than erroring if Money's or Clients' schema files aren't run yet. Now the genuine post-login landing page: `js/auth.js` (sign-in and post-signup redirect) and `js/supabase-client.js` (`redirectIfLoggedIn`, the already-logged-in bounce from login/signup) all changed from `dashboard.html` to `home.html`, and the Boardly logo link on every page (previously pointing to `dashboard.html`) now points to `home.html` too, the standard "logo goes home" convention. The old Good Morning modal (`js/morning.js`) is kept as-is per Charles's explicit choice, both exist for different purposes | Not tested against a real login flow yet | No |
| Command Center (natural language to structured plan to approval) | Planned | No | No | No | No |
| **Lead pipeline, Section 14** (`schema_v66_lead_pipeline.sql`, `clients.pipeline_stage`) | Existing | Applied and confirmed live. A lead is modeled as a client row whose stage isn't yet 'active_client', not a separate table, avoiding the lead-to-client hand-off conversion problem Section 14 itself warns about. Existing clients defaulted to 'active_client' (not 'new'), since they were already real relationships before this migration | n/a | Confirmed via a column check | No |
| **Clients page pipeline UI** (Active/Leads/All tabs, inline stage editor, Add lead button, stage badge in detail view) | Building | n/a | Yes; guarded by `pipelineReady` so the page still works if `schema_v66` isn't run yet (shows everyone as Active client) | Not tested against live data yet | No |
| **Follow-up engine, Section 15** (invoice aging, drafted follow-ups) | Building | n/a (no schema change needed, reuses existing invoice fields) | Yes; a Follow-ups tab on `clients.html` surfaces any sent/viewed/unpaid invoice aged 5+ days (a fixed, stated threshold, not AI), each with a "Draft follow-up" button producing an editable, copyable message. Nothing is ever sent automatically, matching the brief's explicit warning against auto-spamming clients. Scoped to invoices only this pass, proposals don't yet link to a saved client (`client_name` is free text there), extending that is real future work | Not tested against live data yet | No |
| **Marketplace Dispute Center, Phase 6, Section 20** (`schema_v67_marketplace_disputes.sql`) | Existing | Applied and confirmed live (6 dispute columns on `marketplace_bookings`); `marketplace-file-dispute` (client or provider, verified either by access_token or JWT), `marketplace-resolve-dispute` (provider-only, JWT-verified), and `marketplace-booking-status` (updated to return dispute state) all deployed live | Yes; `booking-status.html` shows a real factual timeline (booked/paid/disputed timestamps, no invented evidence per Section 20) and a dispute-filing form for the client; `marketplace.js`'s Bookings tab shows dispute badges and file/resolve actions for the provider | Not tested against a real dispute yet | No |
| **Marketplace trust badges, Section 18** (`marketplace-get-trust-badges`) | Existing | Deployed live, no schema change needed. Every badge is a real, independently-checkable fact, never a score: email verified (from Supabase Auth's own `email_confirmed_at`), payout verified (a row exists in `marketplace_provider_payouts`, meaning Paystack itself resolved a real bank account), completed bookings (a plain count of `status='released'` bookings), member since (profile creation date). Only booleans/counts are exposed, never account numbers or booking details | Yes; shown on the public profile detail view in `marketplace.html`, loaded separately from the profile itself so the page never blocks on it. Not shown on directory grid cards, would mean 60 extra requests per page load | Not tested against live data yet | No |
| **Marketplace reviews, Section 17** (`schema_v68_marketplace_reviews.sql`) | Existing | Applied and confirmed live. A review can only be left for a booking that actually reached `status='released'` (the client themselves already confirmed the work was done), enforced by `marketplace-submit-review` and backstopped by a real unique constraint on `booking_id`, not just application logic. `marketplace_reviews` is publicly readable by design, reviews are meant to be a public trust signal | Yes; a star-rating form appears on `booking-status.html` once a booking is released, and the public profile detail view in `marketplace.html` shows the average rating plus the reviews themselves, read directly via RLS, no Edge Function needed for that part | Not tested against live data yet | No |
| **Operations, Phase 7, Sections 62/63** (`schema_v69_workspace_persona.sql`, `operations.html`/`js/operations.js`) | Existing | Applied and confirmed live: `user_settings.workspace_type` now persists the persona signup already asked for (js/auth.js's SIGNUP_WORK_TYPES step), which previously only tagged the first board once and got discarded. `schema_v70_operations_fix.sql` widened the check constraint to all 9 real work types (the original only listed 7, missing `social_media` and `software`, which already existed in `js/dashboard.js`'s own TERMINOLOGY object) | Yes; Operations groups every board by its real `work_type` across all 9 types, shows a persona picker when there's no signal yet, and a "Change" button lets it be updated any time afterward (never hides boards, only shapes what shows first). Lets a properly-typed board be created directly. Added to mobile nav across all 9 hub pages, a full audit also caught and fixed two pages (`clients.html`, `money.html`) that were missing an Insights link | Not tested against live data yet | No |
| Persona/vertical module system, Section 63 full vision (`enabled_modules`, `default_workflows`, `default_dashboard`) | Planned | No | No | No | No |
| **Silent Sentinel, Phase 8, Section 28** (upgraded `home.html` Attention section) | Existing | n/a, no schema change needed | Yes; every signal is a plain threshold check, never AI (overdue tasks/invoices banded Medium/High/Critical by days overdue, failed payments in the last 7 days, open Marketplace disputes, blocked tasks), sorted by severity, shown as a passive list on Home, never a popup or notification, per Section 28's "don't constantly interrupt" | Not tested against live data yet | No |
| **Reality Mode, Phase 8, Section 29** (added to the existing Project Baselines feature) | Existing | n/a, no schema change, reuses `project_baselines` (schema_v53) and the live `tasks` table | Yes; found "Project Baselines" already covered most of Section 29's spirit (comparing a frozen plan snapshot to the live board, added/removed tickets, shifted due dates), but had no plan-vs-actual percentage or projected finish date. Added a "Reality check" panel to that same modal: Planned-by-now % (of baseline tasks whose due date has passed), Actual progress % (of current tasks done), and a Projected finish date from the board's real completion rate over the last 14 days, honestly showing "not enough data" rather than guessing when nothing has shipped recently. A one-line explanation names the gap and the original deadline, all plain arithmetic, no AI | Not tested against live data yet | No |
| Opportunity Radar, Intelligence Graph, Sections 30-31 | Planned | No | No | No | No |
| **Database performance, Section 75** (`schema_v71_database_performance.sql`) | Existing | Applied and confirmed live. A direct query against `pg_index` found 65+ columns across nearly every table matching Section 75's own categories (user/tenant IDs, status, due dates, created_at) with no index at all, including `boards.user_id`, arguably the single most frequently filtered column in the app. Added the ones tied to real, regularly-queried lists; left a few very small/rarely-listed tables alone since an unused index just slows every future write | n/a | Confirmed via a sample check against `pg_indexes` | No |
| **System health page, Section 77** (`admin-system-health`) | Existing | Deployed live, mirrors `admin-list-users`'s exact `ADMIN_EMAILS` verification. Every number is a real count (failed payments 7d/30d, payments stuck pending 24h+, open Marketplace disputes, overdue invoices platform-wide, security events last 7 days), nothing fabricated, no invented uptime percentage | Yes; a "System health" section on `admin.html`, loads independently so a health-fetch failure never blocks the existing plan-management table | Not tested against live data yet | No |
| Realtime, Section 76 | Existing | Already real and well-scoped: `js/dashboard.js` subscribes to a single `board-${boardId}` channel, not a global one, matching Section 76's own warning against subscribing every screen to every change | n/a | n/a | Yes |
| Entitlements/pricing, Section 70 | Existing | `js/entitlements.js` already is the centralized `can()`-style system the brief asks for (schema_v49), 3 tiers (free/pro/pro_plus) rather than 4. The file's own comment explains why: adding a Business tier with nothing genuinely exclusive behind it yet would be the fake distinction Section 84 itself warns against | n/a | n/a | Yes |
| **Testing, Sections 78-79** (`supabase/tests/rls_policy_audit.sql`) | Existing | The project has no build step or test runner, so a real, actually-runnable equivalent was built instead: a saved, re-runnable SQL audit against Postgres's own policy catalog, verifying the exact assertions Section 78 names (tenant isolation, no client-side write path on money-adjacent tables, no table with RLS on and zero policies). Run live this session, both real checks came back PASS | n/a | n/a | Yes, run and passing as of this session |
| **Data export, Section 80** (Money and Clients CSV export) | Existing | n/a, no schema change | Yes; Invoices, the Ledger, and the Clients list can now each be exported as CSV, the two newest data sets in the app (Money, Clients) previously had no export at all, unlike the board/task export that already existed | Not tested against live data yet | No |
| **Backup and recovery, Section 81** (`docs/BOARDLY_2_BACKUP_RECOVERY.md`) | Existing | n/a, documentation only | n/a | n/a | Depends on which Supabase plan is active, stated honestly in the doc, this can't be verified from the codebase |
| Product language and no-fake-AI, Sections 82-83 | Existing | n/a | Audited this session's own UI copy for hype language (none found) and for anything mislabeled as AI (every "not AI" comment checked, all accurate, no user-facing copy claims AI where none exists) | n/a | Yes |
| **Landing page hero, Sections 66-69** (`index.html`) | Building | n/a | Hero headline, subhead, and CTAs replaced with the brief's exact requested copy ("Do the work. Win the work. Get paid."), the trust-stats strip below it updated from pure kanban-speed stats to reflect Work/Clients/Money/Discover actually existing now. This is the hero section only, the brief's full 14-section landing page rebuild (Section 67: product animation, Business Pulse, Marketplace, Money, Operations, Intelligence, industry workflows, security, pricing, FAQ sections) is real, larger, unstarted work, deliberately not rushed in the same pass | Not tested against live data yet | No |
| **PDF export bug fix** (all 6 download-PDF call sites) | Existing | n/a, no schema change | Charles reported "almost all" PDF downloads silently doing nothing. Root-caused: `money.html` never loaded `js/pdf-export.js` at all (a real bug from this session's own Money work), so its invoice PDF button threw a `ReferenceError`, and separately, all 4 other PDF call sites (Proposals, both Custom Forms PDF buttons, and Money's own) had zero error handling around the export call, meaning ANY failure (a blocked CDN, a slow network, an ad-blocker) failed completely silently with no toast, no console guidance, nothing visible. Fixed both: added the missing script tag to `money.html`, and wrapped every one of the 6 PDF call sites (Money, both Custom Forms buttons, Proposals, Documents, CV Builder, the last two already had it) in try/catch with a real error toast | Not tested against a live browser yet, but every page now confirmed via a script-tag audit to actually load `pdf-export.js` before calling it | No |
| **Proposal richness** (schema_v72: feature groups, why-price, timeline, payment stages, notes, closing) | Building | `schema_v72_proposal_richness.sql` applied live, all columns nullable/defaulted so existing proposals render unchanged | Proposal Builder has new fields for all of it, a repeatable payment-stage list, the PDF export and the public `proposal.html` client view both updated to show these sections when present | Verified with `node --check`, duplicate-ID, div-balance, and dash sweeps this session. Not tested against a live browser yet | No, needs a real end-to-end save/PDF/client-view check |
| **Write with AI** (`generate-proposal-draft`, `generate-cv-draft` edge functions) | Building | Both deployed live, reuse the existing free Groq/OpenRouter secret the board assistant already uses, no new secret needed | Buttons and panels added to the Proposal Builder and CV Builder, drafts fill the form fields for review, nothing auto-saves or auto-sends | `get_advisors` shows no new findings beyond the pre-existing documented ones. Not tested against a live browser yet | No, needs a real generate-and-review test with a live Groq key |
| **Section 94 success-criteria walkthrough** | Existing | n/a, audit only | n/a, audit only | Done as a code-level trace (read the actual functions/schema involved in each of the 4 personas' steps), not a live browser click-through, see `docs/BOARDLY_94_SUCCESS_CRITERIA_WALKTHROUGH.md` for the full per-step table and real gaps found | n/a |
| Retainers (brief Section 12) | Building | `schema_v73_retainers.sql` applied live: `retainers` table plus an optional `invoices.retainer_id` link, both additive | Money's new Retainers tab: create/edit a retainer (client, amount, currency, hours included, billing day, status), shows "due this period" and a one-click "Generate this month's invoice" that creates a completely normal, pre-filled invoice linked back to the retainer | `get_advisors` shows no new findings. Not tested against a live browser yet | No, needs a real create-retainer-then-generate-invoice test. Honesty note: invoice generation is one click, not a background job, this codebase has no scheduler, see the schema file's own comment |
| Proposal Builder character limits, real fix | Existing, fixed | n/a | An earlier pass only removed the character cap from the "Write with AI" brief textarea, missed that the actual Proposal Builder fields themselves (title, client name/email, role, intro, features, line item description, why price, timeline, payment stage label, notes, closing) still had `maxlength` attributes. Charles caught this. All 12 removed from dashboard.html | Verified via duplicate-ID and div-balance checks | No |
| Marketplace booking delete | Building | New `marketplace-delete-booking` edge function, deployed live, JWT-verified, only deletes a booking whose status is `pending_payment` or `cancelled` (never one with real payment history: `paid_held`/`released`/`refunded` are refused with a clear error) | A "Delete this booking" link on the provider's Bookings tab, only shown for the two deletable statuses, with a confirm dialog | `get_advisors` shows no new findings. Not tested against a live browser yet | No |
| Marketplace "find my booking" by email | Existing | `marketplace-find-bookings-by-email` deployed live (was written last session but a tool outage prevented deployment, deployed this session) | booking-status.html's lookup form, marketplace.html's header link | `get_advisors` clean | No |
| Marketplace payment stuck "confirming forever" | Building | New `marketplace-verify-payment` edge function, deployed live: asks Paystack directly ("transaction/verify") instead of only waiting for a webhook that may never arrive (3 separate webhook functions exist in this codebase, `payment-webhook`, `marketplace-payment-webhook`, `invoice-payment-webhook`, but Paystack only calls whichever ONE URL is configured in its own dashboard, if that URL is ever wrong the booking waits forever no matter how correct the webhook code is) | booking-status.html auto-calls this after 15s of no change, plus a manual "check with Paystack directly" button | `get_advisors` clean | No, needs a real stuck-payment test. Charles should also double check Paystack's dashboard webhook URL is set to `payment-webhook`'s URL specifically (the combined router), not one of the other two |
| Signup email confirmation, "no questions asked" | Existing, fixed | No schema change | Found the real cause: `emailRedirectTo` was never set on `signUp()`, so confirming an email fell back to the project's Supabase Auth "Site URL" setting (a dashboard setting, appears to still be pointing at localhost per Charles's screenshot) and skipped signup's own onboarding steps entirely. Fixed the code side: `emailRedirectTo` now points back to signup.html itself with a marker, which now skips the auto-redirect-away-if-logged-in and jumps straight to the work-type question | Not tested against a live browser yet | No. Charles must also fix the Supabase Dashboard side (Site URL and Redirect URLs), code alone can't reach that setting, see reply for the exact steps |
| Admin: permanently delete any account | Building | New `admin-delete-user` edge function, deployed live, gated by the same `ADMIN_EMAILS` check every other admin action uses, reuses delete-account's exact storage-cleanup-then-delete logic, refuses to let an admin delete their own account through this door | A Delete button per row in admin.html's user table, requires typing the exact email to confirm | `get_advisors` clean. Verified every schema file's `user_id` foreign keys, the only non-cascading ones are 5 deliberate attribution-only columns (`assigned_to`, `created_by`, etc.) that correctly use `on delete set null` so a collaborator's own board doesn't vanish when their account is removed, so both this and self-delete are already genuinely thorough wipes | No |
| Discover invisible on desktop | Existing, fixed | n/a | Real bug, confirmed: the Discover link only ever existed in the mobile hamburger menu (`sm:hidden` container) across all 8 pages that have one, never in any page's actual desktop nav row. Added a matching desktop link to all 8 (clients, dashboard, home, money, operations, settings, stats, tools), each using that page's own existing class convention | Verified via duplicate-ID and div-balance checks on all 8 | No |
| Money page mobile tab overflow | Existing, fixed | n/a | Adding the Retainers tab pushed Money's tab row to 5 tabs, which overflowed off the right edge on narrow screens with no way to reach it (no `overflow-x-auto`). Fixed on money.html, and proactively on clients.html too (same wrapper class, would hit the identical bug the next time a tab is added there). Also added `flex-shrink:0; white-space:nowrap` to the shared `.pill-tab` CSS class itself so this class of bug is harder to reintroduce anywhere it's used | Verified via duplicate-ID and div-balance checks on both pages | No, needs a real narrow-screen check |
| Delivery pricing + daily revenue (dispatch, closes another Section 94 gap) | Building | No schema change, uses the existing metadata jsonb column (schema_v14), added a `price` field to VERTICAL_FIELDS.logistics (js/dashboard.js) with real number-type input/collect support added to the vertical-fields system itself | Control Tower's stats line now sums today's completed deliveries into a "collected today" total, and each active delivery row shows its own price when set | Not tested against a live browser yet | No |
| Marketplace job board (schema_v75, resolves the directory-vs-job-board fork) | Building | `schema_v75_marketplace_opportunities.sql` applied live: `marketplace_opportunities` and `marketplace_applications` tables, RLS-only (no edge function needed, matches the existing directory's own pattern), added ALONGSIDE the existing directory rather than replacing it | marketplace.html gets a "Find work" / "Find professionals" toggle, a real post/browse/apply/accept-or-decline flow, "View your postings" so a poster can find their way back to check applications, a `?job=` deep link matching the existing `?u=` one | `get_advisors` shows no new findings (both new tables' RLS policies register correctly, no rls_enabled_no_policy flag). Not tested against a live browser yet | No, needs a real post-then-apply-then-accept test |
| Accepted application to escrow booking (schema_v76, `marketplace-pay-application`) | Building | `schema_v76` added the two link columns (SQL now in the repo). The step that fills them was never built until this session: new `marketplace-pay-application` edge function, deployed live, signed-in poster only. It reads the price from the accepted application (never from the browser), uses the poster's own login email, a fixed return address, needs the applicant to have finished payout setup, creates the escrow booking, links it, and returns Paystack's checkout page. Safe to click twice: one application gets one booking, and a closed payment tab gets the same page back (`schema_v81` stores the checkout link, hidden from signed-in users). Nothing here marks anything paid, the webhook or the verify function does that | `js/marketplace-public.js`: accepted applications show "Pay securely" to the poster, then "Open payment page" and "Payment status" (uses `get_application_booking_link`, poster only). The applicant sees plain status text: accepted, payment started, held, released | Bad token rejected live (401). UI flow checked in a simulated page with a stand-in database (17 checks). Not tested against real Paystack or a real browser, and the applicant needs payout setup first | No, needs a real accept, pay, hold, release test in Paystack Test Mode |
| One hire per job (`schema_v86`, `schema_v87`, `js/marketplace-public.js`, `marketplace.html`) | Building | Accepting closes the job and declines the other waiting applications, a second accept is refused, a hired job cannot be reopened, unique index on one accepted application per job. Applicants can still read a job they applied to after it closes. Applied live | The accept dialog says it closes the job and how many other applications it declines. The page reloads after accepting. Reopen is replaced by a short explanation once someone is hired. Declined applicants see "not chosen, declined or hired someone else" | SQL tested in rolled-back transactions on the live database (6 cases for v86, 5 for v87). Page checked in a simulated page (8 checks, `marketplace-one-hire-ui.test.mjs`). Not tested in a real browser | No, needs the two account browser test |
| Atomic invoice payment confirmation (`schema_v85`, `payment-webhook`, `invoice-payment-webhook`) | Building | One database function confirms the payment and recomputes the invoice in a single transaction with row locks. Webhooks answer 500 on a database failure so Paystack retries. Marketplace hold updates are conditional. All four functions redeployed live | No page change | 12 SQL cases in a rolled-back transaction on the live database, plus webhook wiring tests. Not tested with a real Paystack payment | No, needs a Test Mode payment |
| Onboarding: work-type question | Existing, improved | n/a | Signup already asks "What are you organizing?" right after account creation and persists it (user_settings.workspace_type), an earlier version of the Section 94 walkthrough wrongly called this a complete gap, corrected there. Fixed in that pass: the picker (js/auth.js's SIGNUP_WORK_TYPES) was missing 2 of the 9 real work_type values (Social media, Software), added to match operations.js's OPS_VERTICALS list and dashboard.js's TERMINOLOGY. Also added the two missing vertical cards to features.html and index.html's marketing verticals grids | The two new options render from the same TERMINOLOGY-derived label/icon pair every other work type uses | Not tested against a live browser yet | No |
| Onboarding: second question + Home reordering (brief Sections 62-63) | Building | `schema_v74_onboarding_goals.sql` applied live: `user_settings.goals` (jsonb array), additive | signup.html's new step 3 ("What do you want help with?", multi-select, skippable), persisted the same one-time-consume way as workspace_type. home.js reads it and reorders Home's Money/Work/Clients sections by relevance, Today always stays first, nothing is ever hidden (matches the schema file's own honesty note and Section 89) | `get_advisors` shows no new findings. Not tested against a live browser yet | No, needs a real signup-through-Home test. Still narrower than the brief's full vision: this reorders Home, it doesn't yet gate default_dashboard or enabled_modules more broadly, and there's no Settings control to change goals after signup yet (the signup copy was written to not claim one exists) |

| **AI fill on 5 forms** (`ai-fill-form` edge function, `js/ai-fill.js`) | Building | One shared function for lead, client, invoice, expense, retainer. Signed-in users only, input capped, every field checked against a strict allowlist, never reads or writes the database, small best-effort hourly limit, one structured log line per call | A "Fill with AI" strip under the header of the Client modal (Add lead and New client), Invoice builder, Expense and Retainer modals. Only offered when creating, never when editing. Fills the form, nothing saves | 23 function checks and 21 interface checks pass under Node with stubs, real markup for clients.html and money.html. Charles confirmed on 20 September 2026 that it works well in real use on the live site | Live. Not yet covered by an automated end to end test |
| **Invoice currency fix** (`create-invoice-payment`, `payment-webhook`, `invoice-payment-webhook`) | Building | Currency is now sent to Paystack and checked in both webhooks. Before this, a non-NGN invoice was charged in NGN and could be marked paid | n/a | 10 and 9 checks pass, including a test that fails on the old code | Deployed (live). If the Paystack account does not support a currency, initialize now fails honestly instead of charging the wrong one |
| **Booking token no longer readable by providers** (`schema_v77`, `js/marketplace.js`) | Building | Column privilege hides `marketplace_bookings.access_token` from signed-in users. Edge Functions are unaffected | Bookings tab and Home now list columns explicitly | Tested in a rolled-back transaction on the live database: before, a provider could read the token, after, they cannot | Applied and confirmed live (re-checked 20 Sep 2026). Companion `schema_v78` does the same for applications and is live too |
| **Find my booking now emails links** (`marketplace-find-bookings-by-email`) | Building | Returns no tokens and no booking data, sends the links by Brevo to the typed address, same neutral reply either way, 3 emails per address per hour, fixed site base | `booking-status.html` shows the neutral message | 12 checks pass with stubs | Function deployed (live). Needs the Brevo secrets and the frontend push |
| **AI writers require a signed-in user** (`generate-proposal-draft`, `generate-cv-draft`) | Building | Real user check plus a small hourly limit | n/a | 10 checks pass | Deployed (live) |
| Team: leader role, leader-only approval, team chat (`schema_v79_leader_role_and_chat.sql`) | Building | Applied and confirmed live: `user_is_board_member` now treats `leader` as an editor, new `user_is_board_leader()` function, `boards.approval_requires_leader` column (default false), a trigger on `tasks` plus a matching `approval_history` insert policy that block a non-leader from approving/requesting changes when that setting is on, a new member-only realtime `board_messages` table for team chat. `invite-member` edge function redeployed to accept `role: "leader"` and live-tested (still rejects a bad token) | `dashboard.html` invite dropdown has a Team leader option; a per-board "Only team leaders can approve" checkbox (owner-only, in the invite menu); `js/approval-workflow.js` hides Approve/Request changes for non-leaders when the setting is on and shows a plain explanation instead; new `js/team-chat.js` (panel, send, realtime) with a chat button in the board header. Comments/@mentions and Members/roles/capacity already existed from before this pass | SQL tested in a rolled-back transaction first. Chat panel, sending, escaping and the realtime hook checked in a simulated page (5 checks). Live checks: realtime publication includes `board_messages`, member-only read and post policies present. `schema_v83` adds a 1 to 4000 character limit on messages. Still not tested in a real browser with two accounts | No |
| Job board: re-apply, applicant status, names (`schema_v80`, `js/marketplace-public.js`) | Building | Applicants can edit a pending application; once answered it is locked. Trigger and column privileges described in BOARDLY_SECURITY_2.md F15 and F16. Applying is now a plain insert or update, no upsert | Applicant sees where things stand (waiting, accepted, declined, payment state) instead of a raw database error, poster sees applicant display names (public profiles only), confirm dialogs on accept and decline, no Accept button when there is no price | 10 database cases tested live in a rolled-back transaction, 17 page checks in a simulated page | No |
| Job board: close, reopen and delete your own job (`schema_v82`) | Building | Delete policy already existed (schema_v75). New trigger blocks deleting a job while money is held, pending under 24 hours, or in dispute | Poster gets Close job / Reopen job and Delete job on the job page, both with confirm dialogs, delete refused with a plain message when money is in flight | 5 database cases tested live in a rolled-back transaction, delete and close checked in the simulated page | No |
| Fixed return address for payments (`create-invoice-payment`, `marketplace-create-booking`, `marketplace-pay-application`) | Building | All three use `PUBLIC_APP_URL` or the GitHub Pages address, browser-sent origin removed | `invoice-page.js` and `marketplace-public.js` no longer send an origin | Deployed live. Not tested with a real payment | No |
| Remove an invited person or cancel a pending invite (`schema_v88_remove_board_member.sql`) | Building | Applied live to the Boardly project. `remove_board_member(uuid)` checks the caller owns the board, un-assigns that person's tasks on the board, then deletes the membership, all in one step. Not callable by anonymous users. Tested in a rolled-back transaction: a different signed-in user is refused, the owner succeeds | New "People on this board" list in the invite menu (`dashboard.html`, `js/collaboration.js`): every invited person with their role and a pending label, owner-only Remove button, confirm box, falls back to a plain delete if the function is missing | `supabase/tests/remove-member-ui.test.mjs` (18 checks in a simulated page). Not yet tried in a real browser | No |
| Delete one notification, or clear all (`js/notifications.js`, `dashboard.html`) | Building | No database change. The delete policy on `notifications` already existed and was checked live: an owner can delete only their own rows (tested in a rolled-back transaction) | An X on every notification, a "Clear all" button with a confirm box, buttons hide when there is nothing to act on, errors are shown and not swallowed | `supabase/tests/notifications-clear-ui.test.mjs` (17 checks in a simulated page). Not yet tried in a real browser | No |
| Board people list loads on page load (bug fix in `js/collaboration.js` and `js/dashboard.js`) | Improved | n/a | The member list and header avatars were requested before the board was known, so they stayed empty until a board switch. `loadBoardMembers()` now waits for the readiness check and dashboard.js calls it right after `loadBoards()`. The "People on this board" section is always shown, with a friendly empty message | Race covered in `remove-member-ui.test.mjs`. Not yet tried in a real browser | No |
| Plain-words payout error on the buyer release button (`marketplace-release-payment`, version 16 live) | Building | Paystack refusals about Boardly's own account (starter business, OTP, low balance) are turned into a message that says the payment is safe and still held. Raw text goes to the function logs. Booking still rolls back to `paid_held` | No page change needed, `js/booking-status.js` already shows the message | Message mapping run against 5 sample Paystack messages. Live transfer still needs a Paystack account approved for Transfers | No |
| Rate limits on public endpoints, finding F9 (`schema_v89_rate_limits.sql`, six edge functions) | Building | Applied live. `rate_limit_hit` counts calls per time window, service role only, people counted by a one-way hash of their IP so no raw IP is stored. Live in `submit-request`, `submit-custom-form`, `roadmap-vote`, `respond-to-proposal`, `client-portal-action`, `marketplace-find-bookings-by-email`. Fails open if the counter breaks. Not covered: the public read functions, and one person using many IP addresses | Nothing to see, over-limit callers get a plain "too many requests, wait a few minutes" message (429) | Live check: 12 quick calls gave 10 normal and 2 blocked. `supabase/tests/rate-limit-helper.test.mjs` (13 checks) | No |
| XSS audit and fixes, finding F10 (`js/supabase-client.js` `safeUrl`, 17 escape helpers, `share.html`) | Building | n/a | All 17 escape helpers now escape quotes so they are safe inside attributes. New `safeUrl()` guards every link a person can type (attachment, cover, published, git PR, staging, meeting, portfolio). `toastUndo` uses textContent, custom form field ids escaped, notification links only navigate to http, https or relative | `supabase/tests/xss-guards.test.mjs` (7 checks) and `supabase/tests/xss_audit.py` for review. The audit is a heuristic, not a proof. Not yet tried in a real browser | No |
| Private task attachments, finding F5 (`schema_v90`, `schema_v91`, `js/dashboard.js`, `js/proofing.js`) | Building | Step A applied live 21 Sep 2026 (read function, read rule, `proof_comments.attachment_path` with backfill and trigger). Step B (bucket private) written, NOT applied, waits for the frontend push and a browser test | Files save a stable path, links are signed in one batch when tasks load and renewed on edit, tab focus and every 10 minutes. Old attachments work without a data migration. Proofing pins match on path | Rolled-back SQL test 14 of 14 on the live data, `supabase/tests/private-attachments-ui.test.mjs` 43 of 43 with Supabase stubbed. Not yet tried in a real browser | No |
| `BOARDLY_SECURITY_2.md`, `BOARDLY_PRODUCT_2.md` | Existing | n/a | n/a | n/a | Yes (documents themselves complete) |

**Status key:** Existing (was already true, or is now confirmed live, as of
this session) / Building (in progress this session) / Blocked / Planned
(not started) / Tested / Production Ready.

**What "real Paystack payments" needs to actually go live, beyond the code:**
1. `PAYSTACK_SECRET_KEY` must be set as a secret on the Supabase project
   (the same key Marketplace already uses works here too).
2. The `payment-webhook` function's URL (a single combined router that
   now handles both Marketplace and Money) needs to be added in
   Paystack's dashboard (Settings, API Keys and Webhooks). This replaces
   the earlier plan of needing two separate URLs registered.
3. A real end to end test (create an invoice, send it, pay a small real
   amount, confirm the webhook flips it to paid) has not been run yet.

See `docs/BOARDLY_2_IMPLEMENTATION_MAP.md` for the full Phase 0 audit this
table extends, and `docs/BOARDLY_DESIGN_SYSTEM.md` for the Phase 1 decision
record.
